<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itboyst.facedemo.mapper.ZrecordMapper">

    <insert id="insertZrecord" parameterType="com.itboyst.facedemo.dto.Zrecord" >
               INSERT INTO zrecord (zid, zscheduleID, zteacherID, zdate ,ztype, zcontent)
               VALUES(#{zid}, #{zscheduleID}, #{zteacherID}, #{zdate} ,#{ztype}, #{zcontent});
    </insert>


</mapper>